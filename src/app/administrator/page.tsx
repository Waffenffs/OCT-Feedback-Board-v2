export default function Administrator() {
    // DepartmentsList
    // DepartmentsListItem

    // Fetch the amount of accounts with departments list
    // Fetch the amount of feedbacks associated with each department
    // Display each feedbacks in their categories (Pending, Resolved, Flagged)

    // Admin powers:
    // Respond to reports (there should be a reports section)

    return (
        <div className='w-full h-full p-10'>
            <h1 className='text-3xl font-bold text-slate-900'>Departments</h1>
        </div>
    );
}
